# Middle-ground Codex config: writes confined to the workspace, network on,
# no approval prompts. Blocks out-of-workspace writes but leaks network egress.
approval_policy = "never"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = true
writable_roots = ["/workspace"]
